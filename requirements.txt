# Streamlit and WebRTC
streamlit==1.35.0
streamlit-webrtc==0.45.0

# OpenCV - headless (to avoid GUI conflicts in server/cloud)
opencv-python-headless==4.9.0.80

# TensorFlow CPU-only version
tensorflow-cpu==2.12.0

# AV (for streamlit-webrtc video processing)
av==10.0.0

# Core numerical libraries
numpy==1.24.3

# Optional: safer TOML parser if you’re modifying .toml files
toml==0.10.2